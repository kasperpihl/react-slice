{
  "name": "react-slice",
  "version": "0.0.12",
  "description": "Simple global state with reducers",
  "source": "src/index.js",
  "main": "dist/slice.js",
  "unpkg": "dist/slice.umd.js",
  "scripts": {
    "prebuild": "rimraf dist",
    "prepublishOnly": "npm run build",
    "build": "microbundle --jsx React.createElement --no-compress --no-sourcemap",
    "start": "microbundle watch --jsx React.createElement --no-sourcemap --no-compress"
  },
  "keywords": [
    "Global",
    "state",
    "reducer",
    "react",
    "redux"
  ],
  "author": "Kasper Pihl Tornoe",
  "license": "MIT",
  "devDependencies": {
    "microbundle": "^0.11.0",
    "react": "^16.8.6",
    "rimraf": "^2.6.3"
  },
  "peerDependencies": {
    "react": ">=16.8"
  }
}
